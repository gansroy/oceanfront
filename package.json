{
  "name": "oceanfront-repo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "watch": "lerna run --parallel watch"
  },
  "devDependencies": {
    "lerna": "^3.16.4"
  }
}
