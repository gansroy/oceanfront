{
  "name": "oceanfront-repo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "clean": "lerna run clean",
    "watch": "yarn workspace oceanfront run build-dev && lerna run --parallel watch"
  },
  "devDependencies": {
    "lerna": "^3.16.4"
  }
}
