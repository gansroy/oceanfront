{
  "name": "oceanfront-repo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "clean": "lerna run clean",
    "coverage": "lerna run coverage",
    "demo": "yarn workspace oceanfront run build-dev && lerna run --parallel demo",
    "lint": "lerna run lint",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.16.4"
  }
}

